Pages demonstrate how to lay out html-page and animate elements.
There are few types of page layout: normal flow (block and inline elements), tables, float layout, position, columns, flexible box, grid layout.
